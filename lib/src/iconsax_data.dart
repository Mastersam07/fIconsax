import "package:flutter/widgets.dart";

class IconsaxData extends IconData {
  const IconsaxData(int codePoint, String fontFamily)
      : super(codePoint, fontFamily: fontFamily, fontPackage: "ficonsax");
}
